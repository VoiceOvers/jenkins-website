var helper = require('../../helper');

var cleanup = helper.cleanup,
    expect = require('chai').expect,
    should = require('chai').should(),
    StoreAdminPage = helper.pages.storeadmin;
    user = helper.mocks.user;


describe('E2E: Admin Products', function () {

  // before(function () {

  // });
});
